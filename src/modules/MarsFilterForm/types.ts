interface MarsFilterProps {
  roverValue: string;
  solValue: number | number[];
  setRoverValue: (prop: string) => void;
  setSolValue: (prop: number | number[]) => void;
}

export { type MarsFilterProps };
